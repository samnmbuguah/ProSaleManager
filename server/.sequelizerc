const path = require('path');

const isProd = process.env.NODE_ENV === 'production';
const base = isProd ? 'dist/src' : 'src';

module.exports = {
  'config': path.resolve(base, 'config', 'config.cjs'),
  'models-path': path.resolve(base, 'models'),
  'seeders-path': path.resolve(base, 'database', 'seeders'),
  'migrations-path': path.resolve(base, 'database', 'migrations')
};
