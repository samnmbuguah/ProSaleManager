# Environment Configuration
# Copy this file to .env and fill in your values

# ============================================
# Basic Configuration
# ============================================
NODE_ENV=development
PORT=5000
HOST=0.0.0.0
CLIENT_URL=http://localhost:5173

# ============================================
# Security Secrets
# ============================================
JWT_SECRET=your-jwt-secret-key-here
SESSION_SECRET=your-session-secret-key-here
COOKIE_SECRET=your-cookie-secret-key-here

# ============================================
# Database Configuration
# ============================================

# For Development (SQLite) - Used when NODE_ENV=development or NODE_ENV=test
# SQLite database file path
SQLITE_PATH=database.sqlite

# For Production (MySQL) - Used when NODE_ENV=production
# MySQL/MariaDB connection settings for cPanel production
# You can use either DB_* or MYSQL_* prefix (both are supported)
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_mysql_username
DB_PASSWORD=your_mysql_password
DB_NAME=your_mysql_database

# Alternative MySQL variable names (also supported)
# MYSQL_HOST=localhost
# MYSQL_PORT=3306
# MYSQL_USER=your_mysql_username
# MYSQL_PASSWORD=your_mysql_password
# MYSQL_DATABASE=your_mysql_database

# Database logging (optional, default: false)
# DB_LOGGING=false

# ============================================
# Database Backup Configuration (Production Only)
# ============================================
# Google Drive backup settings - Required for automated daily backups in production
# The backup service runs automatically at midnight (00:00) in production mode

# Google Drive Folder ID where backups will be uploaded
# To get this: 
# 1. Create a folder in Google Drive
# 2. Open the folder in Google Drive
# 3. The folder ID is in the URL: https://drive.google.com/drive/folders/FOLDER_ID_HERE
GOOGLE_DRIVE_FOLDER_ID=your-google-drive-folder-id

# Google Drive Service Account Credentials (JSON string)
# To set this up:
# 1. Go to Google Cloud Console (https://console.cloud.google.com/)
# 2. Create a new project or select existing one
# 3. Enable Google Drive API
# 4. Create a Service Account
# 5. Generate a JSON key for the service account
# 6. Share the Google Drive folder with the service account email (found in JSON)
# 7. Paste the entire JSON content here as a single-line string (escaped)
# Example format (must be on one line with escaped quotes):
# GOOGLE_DRIVE_CREDENTIALS={"type":"service_account","project_id":"your-project",...}
GOOGLE_DRIVE_CREDENTIALS=your-google-drive-service-account-json

# Backup retention days (optional, default: 7)
# Number of days to keep backups before cleanup
BACKUP_RETENTION_DAYS=7

# Backup timezone (optional, default: UTC)
# Timezone for the midnight backup schedule
# Examples: "America/New_York", "Europe/London", "Asia/Tokyo", "UTC"
BACKUP_TIMEZONE=UTC

# ============================================
# Cloudinary Configuration (for image uploads)
# ============================================
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# ============================================
# External APIs
# ============================================
PEXELS_API_KEY=your-pexels-api-key

# ============================================
# Printer Configuration
# ============================================
LAN_PRINTER_IP=192.168.1.87
LAN_PRINTER_PORT=9100
CUPS_PRINTER=Thermal

# ============================================
# User Credentials (for initial seed data)
# ============================================
# These credentials are used when seeding the database
# Change these to secure values in production

# Eltee Store Users
ELTEE_ADMIN_EMAIL=admin@example.com
ELTEE_ADMIN_PASSWORD=ChangeMe123!
ELTEE_MANAGER_EMAIL=manager@example.com
ELTEE_MANAGER_PASSWORD=ChangeMe123!
ELTEE_CASHIER_EMAIL=cashier@example.com
ELTEE_CASHIER_PASSWORD=ChangeMe123!

# Nairobi Store Users
NAIROBI_ADMIN_EMAIL=nairobi.admin@example.com
NAIROBI_ADMIN_PASSWORD=ChangeMe123!
NAIROBI_MANAGER_EMAIL=nairobi.manager@example.com
NAIROBI_MANAGER_PASSWORD=ChangeMe123!
NAIROBI_CASHIER_EMAIL=nairobi.cashier@example.com
NAIROBI_CASHIER_PASSWORD=ChangeMe123!

# Demo Store Users
DEMO_ADMIN_EMAIL=demo.admin@example.com
DEMO_ADMIN_PASSWORD=ChangeMe123!
DEMO_MANAGER_EMAIL=demo.manager@example.com
DEMO_MANAGER_PASSWORD=ChangeMe123!
DEMO_CASHIER_EMAIL=demo.cashier@example.com
DEMO_CASHIER_PASSWORD=ChangeMe123!

# ============================================
# Notes:
# ============================================
# - In DEVELOPMENT: Only SQLITE_PATH is required
# - In PRODUCTION: DB_USER, DB_PASSWORD, and DB_NAME are required
# - The application automatically selects SQLite for dev and MySQL for production
# - Do NOT commit .env file to version control
# - Change all default passwords before deploying to production
# - Use strong, unique passwords for all user accounts
